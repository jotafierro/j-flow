---
name: Bug report
about: Something in j-flow behaved unexpectedly
title: ""
labels: bug
---

**Command involved** (e.g. `/j-flow-build --fix`, `/j-flow-scaffold --review`):

**Expected behavior:**

**Actual behavior:**

**Claude Code version** (`claude --version`):

**j-flow version** (`.claude-plugin/plugin.json` `version`, or `git describe --tags` in the plugin dir):

**Relevant files** (e.g. `.specs/{slug}/gate-context.md`, `PRODUCT.md` — paste the relevant excerpt, not the whole file):
