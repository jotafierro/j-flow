# Gate Log — {slug}
<!-- Prose in the project's Spec language; headings, IDs, gate vocabulary and code stay English — see references/language-contract.md -->

> Append-only history. Blocks that left `gate-context.md` land here verbatim, newest last —
> superseded by a re-approval, or removed by `/j-flow-reopen`. Never rewrite or prune this file.
> Read by `/j-flow-finish` for the feature README's decision history.
