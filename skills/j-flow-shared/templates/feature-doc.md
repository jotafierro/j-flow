# {Feature Name}

> {One-sentence summary of what the feature enables. User-facing, not technical.}

**Added:** {YYYY-MM-DD}
**Slug:** {slug}

---

## What it does

{2-4 sentences from functional-spec Purpose section, rewritten for users and stakeholders.
Avoid technical terms (no "JWT", "MongoDB", "HTTP 302"). Focus on what the user can do
and why it matters to them.}

## Who it's for

{Feature users from functional-spec.md, rewritten as user personas or roles.}

## Capabilities

{Each AC from functional-spec rewritten as a user-facing capability bullet.
Format: "**{AC short name}**: {what the user can now do / what they experience}."}

- **{AC-1 short name}**: {user benefit, one sentence}
- **{AC-2 short name}**: {user benefit, one sentence}
- **{AC-3 short name}**: {user benefit, one sentence}

## Related

- Technical architecture: [docs/architecture/{slug}.md](../architecture/{slug}.md)
- Internal spec: `.specs/{slug}/functional-spec.md`
